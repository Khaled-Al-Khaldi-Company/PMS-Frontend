/**
 * PrintFooter — فارغ عمداً
 * الورقة الرسمية تظهر كخلفية للصفحة (body::before في globals.css)
 * معلومات التواصل موجودة في أسفل الورقة الرسمية تلقائياً
 */
export default function PrintFooter() {
  return null;
}
